name: DevOps CI/CD Pipeline

on:
  push:
    branches:
      - main

env:
  IMAGE_NAME: devops-flask-app

jobs:

  build-test:
    runs-on: ubuntu-latest

    steps:

    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: 3.9

    - name: Install Dependencies
      run: |
        pip install -r requirements.txt
        pip install pytest

    - name: Run Tests
      run: pytest

    - name: SonarQube Scan
      uses: SonarSource/sonarqube-scan-action@master
      env:
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        SONAR_HOST_URL: http://your-sonarqube-ip:9000

  docker-build:

    needs: build-test
    runs-on: ubuntu-latest

    steps:

    - name: Checkout
      uses: actions/checkout@v3

    - name: Login to DockerHub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USER }}
        password: ${{ secrets.DOCKER_PASS }}

    - name: Build Docker Image
      run: |
        docker build -t $IMAGE_NAME:${{ github.sha }} .

    - name: Push Image
      run: |
        docker tag $IMAGE_NAME:${{ github.sha }} yourdockerhub/$IMAGE_NAME:${{ github.sha }}
        docker push yourdockerhub/$IMAGE_NAME:${{ github.sha }}

    - name: Update K8s Repo
      run: |
        git clone https://github.com/GoravaSivakumar/sample-devops-k8s.git
        cd sample-devops-k8s
        chmod +x update-image.sh
        ./update-image.sh ${{ github.sha }}